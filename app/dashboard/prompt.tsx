export default function Prompt() {
  // use react hook form to create prompt form
  // validate prompt form
  // values
  // prompt
  // tokens to use (default to 1)
  // code generation (default to 0)
  return (
    <div>
      Prompt
      {/* textarea
         checkbox
        number input
        submit button */}
    </div>
  );
}
